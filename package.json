{
  "name": "collection-monitoring-system",
  "private": true,
  "type": "module",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -k -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "create-admin": "npm run create-admin --workspace server",
    "migrate:data": "tsx scripts/migrate-postgres-to-mysql.ts",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "build:client": "npm run build --workspace client",
    "test": "npm run test --workspace server && npm run test --workspace client"
  },
  "devDependencies": {
    "@types/pg": "8.20.0",
    "concurrently": "^9.2.1",
    "dotenv": "16.4.7",
    "mysql2": "3.15.3",
    "pg": "8.20.0",
    "tsx": "^4.19.2"
  }
}
